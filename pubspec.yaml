name: bunhut_pos
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4

  # State Management & Utilities
  provider: ^6.1.2
  intl: ^0.19.0
  google_fonts: ^6.2.1
  flutter_slidable: ^3.1.1

  # Internet Connectivity
  connectivity_plus: ^7.0.0
  logger: ^2.7.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # පසුව Assets (පින්තූර) එකතු කිරීමට අවශ්‍ය නම් මෙම කොටස පාවිච්චි කරන්න:
  # assets:
  #   - assets/images/